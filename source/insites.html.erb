---
title: inSites
subtitle: Promoting a better understanding of the web.
description: Cascading Media's blog.
---

<% blog.articles.each do |article| %>
  <section class="insite-snippet">
    <header>
      <h1>
        <a href="<%= article.url %>"><%= article.title %></a>
      </h1>
      <h3>
        <time><%= article.date.strftime('%b %e %Y') %></time>
      </h3>
    </header>

    <article>
      <%= article.summary %>
    </article>

    <footer>
      <%= link_to("Read Full Article", article.url, class: "button") %>
    </footer>
  </section>
<% end %>
